import styled from "styled-components"

export const Note = styled.p`
    position: absolute;
    bottom: -15px;
    left: 15px;
    width: 100%;
    color: var(--text-light);
    font-size: .6rem;

    span {
        margin-right: 5px;
    }
`