import styled from "styled-components"

export const Anchor = styled.a`
    position: fixed;
    bottom: 35px;
    right: 35px;
    z-index: 900;

    img {
        object-fit: contain;
        pointer-events: none;
    }
`