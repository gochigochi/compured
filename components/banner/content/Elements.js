import styled from "styled-components"

export const Container = styled.div`
    position: absolute;
    inset: 0;
    padding: 35px;
    display: grid;
    gap: 10px;
    align-content: end;
    max-width: 730px;
`